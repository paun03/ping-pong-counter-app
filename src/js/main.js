import * as EventListeners from "./eventListeners.js";

// FUNCTIONS / EVENT LISTENERS

EventListeners.btnPlayerOneEventListener();
EventListeners.btnPlayerTwoEventListener();
EventListeners.btnResetEventListenerFunction();